package com.hospitalmanagement;

import java.io.File;

public class PdfUtil {
    // Stubbed out: PDF export not available in this build (CSV export remains).
    public static boolean exportAppointmentsPerDoctorPDF(File file) {
        System.out.println("PDF export is not enabled in this build. Use CSV export instead.");
        return false;
    }
}
